# annoy.py — fake stub so fashion-clip's unused AnnoyIndex import doesn't crash.
# We never actually use fashion-clip's nearest-neighbor search feature,
# so this class is never really called — it just needs to exist.

class AnnoyIndex:
    def __init__(self, *args, **kwargs):
        pass